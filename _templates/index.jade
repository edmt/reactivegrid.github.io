//-YAML
|---
|layout: default
|---

{% for post in site.posts %}
.post
  h2.post-title
    a(href='{{ post.url }}') {{ post.title }}
  .post-data
    span by
      a(href='#'){{post.author }}
      | — {{ post.date | date_to_string }}
  .post-body
    {{ post.content }}
{% endfor %}
