//-YAML
|---
|layout: default
|title: Archive
|---

#archive-container
  ul
    {% for post in site.posts %}
    li
      span {{ post.date | date_to_string }} —
      a(href='{{ post.url }}')  {{ post.title }}
  {% endfor %}
