//-YAML
|---
|layout: default
|title: Archive
|---

#archive
  ul
    {% for post in site.posts %}
    li
      span {{ post.date | date_to_string }} —
      a(href='{{ post.url }}')  {{ post.title }}
  {% endfor %}
